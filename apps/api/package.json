{
  "name": "@fantasy-oscars/api",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm --prefix ../../packages/shared run build",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "nominees:load": "tsx src/scripts/load-nominees.ts --file ../../db/fixtures/golden-nominees.json",
    "test": "vitest run",
    "test:unit": "vitest run -c vitest.unit.config.ts",
    "test:integration": "vitest run -c vitest.integration.config.ts",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "lint": "eslint .",
    "format": "prettier -w . --ignore-path ../../.prettierignore",
    "format:check": "prettier -c . --ignore-path ../../.prettierignore"
  },
  "dependencies": {
    "@fantasy-oscars/shared": "file:../../packages/shared",
    "express": "^4.22.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^11.11.0",
    "@types/express": "^4.17.25",
    "@types/node": "^25.0.3",
    "@types/pg": "^8.11.10",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
