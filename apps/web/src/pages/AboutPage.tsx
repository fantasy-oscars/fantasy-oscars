import { StaticPage } from "../ui/StaticPage";

export function AboutPage() {
  return (
    <StaticPage title="About">
      <p>
        Fantasy Oscars is a lightweight draft game for awards season. Create a league,
        invite friends, draft nominees, and follow standings as winners are set.
      </p>
    </StaticPage>
  );
}

