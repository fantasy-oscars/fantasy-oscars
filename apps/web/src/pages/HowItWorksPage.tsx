import { StaticPage } from "../ui/StaticPage";

export function HowItWorksPage() {
  return (
    <StaticPage title="How It Works">
      <p>
        Create a league, start a season for an active ceremony, invite friends, then draft
        nominees to build your roster.
      </p>
      <p className="muted">
        This page will get more detailed once the draft and scoring UX settles.
      </p>
    </StaticPage>
  );
}
