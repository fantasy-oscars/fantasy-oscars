// JS/TS-only draft header tokens.
// Draft header layout is brittle; keep all pixel thresholds centralized so we can
// tune behavior without hunting magic numbers across components.

export const DRAFT_HEADER_SIDE_PADDING_PX = 14;
export const DRAFT_HEADER_HYSTERESIS_ENTER_PX = 10;
export const DRAFT_HEADER_HYSTERESIS_EXIT_PX = 22;
export const DRAFT_HEADER_NONCOMPACT_EXTRA_PX = 72;

export const DRAFT_BUCKLE_MAX_PX = 360;
export const DRAFT_BUCKLE_MIN_PX = 200;
export const DRAFT_BUCKLE_VW_FRACTION = 0.25;
