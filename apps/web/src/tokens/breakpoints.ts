// Centralized breakpoints for JS media queries and layout decisions.
// Note: CSS media queries still hardcode values; keep these in sync with baseline.css.
export const FO_BP_MOBILE_MAX_PX = 500;
export const FO_BP_NARROW_DESKTOP_MAX_PX = 900;

